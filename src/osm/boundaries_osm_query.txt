/*
This has been generated by the overpass-turbo wizard.
The original search was:
“boundary=postal_code and type=boundary in Budapest”
*/
[out:json][timeout:25];
// fetch area “Budapest” to search in
area(3601244004)->.searchArea;
// gather results
(
  // query part for: “boundary=postal_code and type=boundary”
  node["boundary"="postal_code"]["type"="boundary"](area.searchArea);
  way["boundary"="postal_code"]["type"="boundary"](area.searchArea);
  relation["boundary"="postal_code"]["type"="boundary"](area.searchArea);
);
// print results
out body;
>;
out skel qt;